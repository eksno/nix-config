# Disable Hyprland animations on tofi's layer-shell surface — the global
# `animation = fade, 1, 10, default` adds ~1s of fade-in latency, which
# is the perceived "tofi takes a second to open."
layerrule = noanim, tofi
