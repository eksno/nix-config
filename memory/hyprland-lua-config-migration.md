---
type: project
title: Hyprland .conf (hyprlang) is removed in 0.57 — repo must migrate to Lua
created: 2026-08-18
---

# Hyprland retires `.conf` — hard deadline at 0.57 (MIGRATION DONE 2026-08-25, commit `5ce0c7c`)

Upstream replaced the hyprlang `.conf` format with a Lua 5.4 config runtime.

| Version | State |
|---|---|
| 0.55 | `hyprland.lua` introduced; hyprlang **deprecated**, no new features |
| 0.56.1 | Compositor shows an on-screen **deprecation notice** for `.conf` (PR #15538) |
| **0.57** | **`.conf` removed.** PR #15538 body: *"Adds a notification about .conf being removed starting 0.57"* |

Sources: <https://hypr.land/news/26_lua/>, <https://wiki.hypr.land/Configuring/Start/>,
<https://github.com/hyprwm/Hyprland/pull/15538>

## Status: migrated and running in production

Confirmed on verse after a reboot 2026-08-25:
`[cfg] Using lua config found at /home/eksno/.config/hypr/hyprland.lua`, 62 binds, empty
`configerrors`, eDP-1 applied at 2880x1800@59.97 scale 2 from the migrated modeline, and
`hl.on("hyprland.start")` + `hl.window_rule` placing kitty/discord/protonvpn on the right
workspaces. `hyprland.conf` is gone from `~/.config/hypr` entirely.

Still unverified: Super+LMB drag / Super+RMB resize (see the `mouse` flag note below).

All 71 `.conf` files under `dotfiles/default/hypr/` are now `.lua`, and
`system/lib/dotfiles.nix` emits `hyprland.lua` with `require()` lines instead of
`hyprland.conf` with `source =`. The `.conf` tree was deleted (git history keeps it).

## Hard-won facts (all verified on verse, 0.56.2 — not guesses)

- **`require()` paths are root-relative** to the directory holding `hyprland.lua`
  (`~/.config/hypr`), *including from nested modules*. A file three levels deep still
  writes `require("shared/utility/misc")`, never a sibling-relative path.
- **Globals DO cross the require boundary**, and **`require()` returns the module's
  value**. Both were tested with `--verify-config` and deliberate `error()` sentinels.
  The palette (`shared/colors/startino-neptune.lua`) uses the return-value form.
- **`Hyprland --verify-config -c <file>`** parses a config without starting a
  compositor. Cheapest possible check; use it on every host/user pair.
- **A nested Hyprland is a safe, real validator.** Inside an existing session
  `WAYLAND_DISPLAY` is set, so `Hyprland -c <lua>` picks the *Wayland* backend and
  never touches DRM. Then `HYPRLAND_INSTANCE_SIGNATURE=<sig> hyprctl binds -j` /
  `getoption` lets you diff the new config against the live one. Do NOT try a DRM
  nested instance — see [[xr-hyprland-lease-hotplug-stall]] for what CRTC contention does.
- **Lua binds report `dispatcher: "__lua"`** with an opaque callback id, and
  **`mouse: false` even for mouse binds**. Diff on `(modmask, key, keycode)` instead.
  `HL.BindOptions` in the shipped stubs has no `mouse` field at all, yet upstream's own
  `example/hyprland.lua` passes `{ mouse = true }` — the held-drag behaviour comes from
  the dispatcher (`hl.dsp.window.drag()`), not the flag.
- **The config manager is chosen once, at startup.** `hyprctl reload` will NOT move a
  running `.conf` session onto `hyprland.lua`; that needs a fresh session.
- **A live legacy session regenerates a stub `hyprland.conf`** if the file is deleted
  out from under it ("This config is a STUB! This should never be generated."). Inert —
  `.lua` wins when present — and the activation script clears it each rebuild.
- The `.conf` `**` glob was **not** recursive: `shared/workflow/default/**.conf` never
  pulled in `binds/*.conf`. Verified by bind count (62 active vs 258 if all keymaps
  had loaded). The Lua version enumerates requires explicitly.

## Why this repo was exposed

- All ~1230 lines under `dotfiles/default/hypr/**.conf` are hyprlang across 6 hosts / 7 users.
- `system/lib/dotfiles.nix:136-145` composes `hyprland.conf` from two `source =` lines.
- `./update.sh` runs `nix flake update` **unconditionally**, so the 0.57 bump arrives
  with no gate. Every host boots to an empty config the day nixpkgs lands it.
- As of 2026-08-18 nixpkgs master is still `0.56.2` — runway exists, but no warning shot.

## Migration facts

- New path: `$XDG_CONFIG_HOME/hypr/hyprland.lua`. Hyprland picks `.lua` if present, else falls
  back to `.conf`. Checked **once at startup** — log line: `[cfg] Lua config not found, using legacy config`.
- `source =` → lua `require("path/to/file")`; supports `/` or `.` separators, wildcards
  (`require("./stuff/*")`) and absolute paths. Each `require()` is its own error scope.
- API surface is fully typed in shipped stubs: `/run/current-system/sw/share/hypr/stubs/hl.meta.lua`
  (1777 lines, autogenerated). Point an LSP at it via `.luarc.json`.
- Upstream example: `/run/current-system/sw/share/hypr/hyprland.lua`.
- `hyprctl repl` / `hyprctl eval` only work under the Lua config manager — on a `.conf`
  session they return *"eval is only supported with the lua config manager"*. Useful as a
  one-command check for which manager is live.
- Shape: `hl.config({ general = { ... } })`, `hl.bind("SUPER + Q", hl.dsp.exec_cmd("kitty"))`,
  `hl.monitor({...})`, `hl.window_rule({ name=, match={}, ... })`, `hl.env("K","V")`,
  `hl.on("hyprland.start", function() hl.exec_cmd("waybar") end)`, `hl.animation({leaf=...})`,
  `hl.curve(name, {type="bezier"|"spring", ...})`.
- `hyprconf2lua` (PyPI) exists as an automated converter — unvetted, not used here.

## Already-dead options still in the repo (0.56 removed these)

Found by diffing every `section:key` in the tree against `hyprctl descriptions -j`.
All in `teto`'s chain (hosts `lappy`/`chrono`) — not loaded on verse/lewis, so `hyprctl
configerrors` is silent here and hides them:

- `decoration:drop_shadow|shadow_ignore_window|shadow_offset|shadow_range|shadow_render_power|col.shadow`
  → moved into the `decoration:shadow { enabled, ignore_window, offset, range, render_power, color }` block
- `dwindle:pseudotile`, `dwindle:no_gaps_when_only` → removed
- `master:no_gaps_when_only`, `master:always_center_master` → removed
- `misc:vfr` → `debug:vfr` (see [[../FIXES.md]] 2026-05-23)
- `windowrulev2 =` (teto) → `windowrule` block form

Related: [[nixos-rebuild-flow]], [[lewis-host-profile]]
